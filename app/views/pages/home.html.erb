<%= render 'shared/carousel' %>
<div class="container-fluid">
   <h2 class="mt-5 mb-3">Our favorite kitchens</h2>
  <div class="card-deck mt-5 rounded">
    <% @kitchens.each do |kitchen| %>
       <%= render 'components/card', kitchen: kitchen %>
    <% end %>
  </div>
</div>
