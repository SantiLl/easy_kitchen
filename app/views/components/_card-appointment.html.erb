
<div class="card-trip">
  <img src="<%= @appointment.kitchen.photo %>" />
  <div class="card-trip-infos">
    <div>
      <h1><%= @appointment.kitchen.name %> </h1>
      <h2><i class="fas fa-map-marker-alt"></i> <%= @appointment.kitchen.address %></h2>
      <p><i class="fas fa-calendar-day"></i> <%= @appointment.date %></p>
    </div>
    <h2 class="card-trip-pricing"><i class="fas fa-dollar-sign"></i><%=@appointment.kitchen.price%></h2>
      </div>
</div>
